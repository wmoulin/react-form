const path = require("path");

module.exports = {
    type: "composant",
    authorizedPrerelease: "true",

    gulpTasks: function (gulp, project, conf, helper) {

    },
    externalModules: {
        enabled: false,
        directories: []
    },
    config: {
    }
};